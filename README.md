# koji_MapBundle
koji_MapBundle packages selected QGIS layers, layer styles, SVG symbols, and print layouts into a ZIP file, and imports received bundles into the current project. Imported GeoPackage data is copied next to the current QGIS project file so project layers keep stable local data sources.
